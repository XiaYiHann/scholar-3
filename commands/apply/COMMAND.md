---
name: Apply
description: Apply 模式（逐条执行 tasks.md，并更新 report + ledger）
category: Research
tags: [apply, execution, reproducibility]
---

进入 `/apply`：选择一个 proposal（`research/proposals/Pxx-*/`），逐条执行 `tasks.md` 的未完成项。
规则：
- 只有输出文件存在才允许勾选 checkbox
- 必须同步更新 `experiment-report.md` 与 `research/evidence-ledger.md`
